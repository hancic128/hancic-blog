# 安全政策

## 支持的版本

以下版本会收到安全更新：

| 版本 | 支持状态 |
|---|---|
| 最新 release | ✅ 支持 |
| 更早版本 | ❌ 不支持，请升级 |

本项目通过 GitHub Actions 持续构建并部署 `latest` 镜像，建议始终使用最新版本。

## 报告漏洞

**请勿公开提交安全漏洞相关的 issue 或讨论**。请通过以下方式私密报告：

- **GitHub Security Advisory**：在仓库页面选择 *Security → Report a vulnerability*（推荐）
- **邮件**：直接联系维护者（见仓库主页联系方式）

报告中请包含：

1. 漏洞类型与影响描述
2. 复现步骤（尽量最小化）
3. 受影响版本与修复建议（如有）

我们会在收到报告后尽快回复，并在修复后公开致谢（除非报告者要求匿名）。

## 安全设计说明

- 后台会话基于服务端签名 Cookie（`tower-sessions`），密码使用 Argon2 哈希存储
- 所有数据库访问参数化，无 SQL 注入面
- 上传文件限制类型与大小，图片可选自动压缩
- 前台模板经过 Tera 转义，用户内容渲染为 Markdown 后输出
- 主题上传校验 `theme.toml` 名称白名单，防路径穿越
- 建议生产环境通过反向代理（HTTPS）对外提供访问
